<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Inherit the default Spring Boot logging properties and conversion rules -->
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <!-- Console Appender (inherits settings from defaults.xml) -->
  <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

  <!-- File Appender (inherits settings from defaults.xml) -->
  <include resource="org/springframework/boot/logging/logback/file-appender.xml"/>

  <!-- Root logger configuration -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>

  <!-- Specific logger for a package with a different level and no additivity -->
  <logger name="com.yourcompany.yourapp.controller" level="DEBUG" additivity="false">
    <appender-ref ref="CONSOLE"/>
  </logger>
</configuration>